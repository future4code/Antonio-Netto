export type user = {
   id: string
   name: string
   nickname: string
   email: string
   address: string
}

export type addressInfo = {
   rua: string,
   bairro: string,
   cidade: string, 
   estado: string
}